name: CI
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run build
        env:
          MONGODB_URI: mongodb://localhost:27017/ci
          SESSION_SECRET: ci-somente-para-build-nao-usar-em-producao-123456
